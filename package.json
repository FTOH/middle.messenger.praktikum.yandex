{
  "name": "middle.messenger.praktikum.yandex",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "predev": "rimraf dist .parcel-cache",
    "dev": "parcel src/pages/*.hbs --no-cache",
    "build": "parcel build src/pages/*.hbs",
    "start": "npm run build && node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "express": "^4.18.1",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  }
}
